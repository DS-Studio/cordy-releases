# 更新日志

Cordy 系列产品的公开发布历史。每个产品一个章节，新版本在前。

只有每个产品的最新一次发布会附带可下载的构建——参见
[README.zh-CN.md](README.zh-CN.md）。此处列出但没有下载链接的版本属于产品历史，仅作参考。

日期为各产品记录的发布日期。English version: [CHANGELOG.md](CHANGELOG.md）。

## Cordy 桌面端

### 0.5.3 — 2026-09-17

- 新增 macOS（Apple Silicon）与 Linux（x86_64 与 arm64）原生安装包，通道与 Windows 完全一致：macOS 提供 `.dmg`/`.zip`，Linux 提供 `.AppImage`/`.deb`，可在同一台机器上并存安装，各自数据独立。
- 从 Finder 打开 Cordy 时，macOS 终端与 coding-agent 启动现在能可靠启动。
- 重启后台 runtime 后终端保持可用，agent 状态显示准确，不再留下虚假的"运行中"agent。
- 终端创建超时后不再留下隐藏运行的 shell 进程。
- macOS 上 coding-agent 终端界面聚焦时，Cmd+Q 现在也能正常退出 Cordy。
- Windows 安装包仍未进行代码签名，首次运行时 Windows SmartScreen 会显示"未识别应用"提示。
- macOS 版本使用本地开发签名身份，未经过公证。
- Linux 上的沙盒启动与 AppImage 挂载在普通桌面环境下尚未完全验证。

### 0.5.2 — 2026-09-15

- 新增快捷键 Ctrl+Alt+T，用于打开新终端标签页，与已有的关闭快捷键 Ctrl+Alt+W 成对。
- 终端里的 shell 正常退出时（输入 `exit`，或按 Ctrl+D / Ctrl+C），对应标签页现在会自动关闭，与 Windows Terminal、VS Code 的行为一致。
- 如果 shell 意外崩溃或被外部关闭，标签页现在会保留并显示退出状态，带有"重启"与"关闭"选项，而不是悄悄失效。
- 固定（pinned）的终端标签页永远不会被自动关闭；使用"Stop all sessions"退出 Cordy 时，也不会再有标签页在退出过程中被意外关闭。

### 0.5.1 — 2026-09-14

- 性能指示器与其总览现在把内存、GPU、CPU 与进程数拆分成可独立开关的指标，不再是一个需要自己心算的合计数字。
- 进程总览现在是一张表格，列出最耗资源的进程，每一项都用与内存图表对应的颜色标记，并附带合计行，以及单独一行显示 runtime、shell 与 agent 的合计。

### 0.5.0 — 2026-09-14

- 快捷键（切换、跳转、关闭与复制工作区、TUI 缩放等）现在打开 Cordy 后立即生效，不用先点进终端或切换一次工作区。
- 文件编辑器现在会按面板宽度自动折行长行，适用于所有文件类型。
- General 设置页现在采用与其他设置页一致的版式与样式。
- 从 Git History 打开的 diff 标签页现在显示对应文件自己的图标，而不是通用图标。
- 性能指示器现在把 GPU 进程内存与应用其余部分的内存分开显示。
- Windows 安装包仍未进行代码签名，首次运行时 Windows SmartScreen 会显示"未识别应用"提示（选择"更多信息 → 仍要运行"即可）。
- 详细性能监控面板的 Workload 视图仍仅限 Windows；macOS 与 Linux 只显示应用内存合计。

### 0.4.0 — 2026-09-12

- 终端现在支持 Claude Code、Codex 等 CLI 依赖的键盘协议，Shift+Enter（Codex 中还有 Ctrl+J）现在会插入换行，而不是直接发送消息。
- 请求鼠标输入的 TUI 现在能直接收到点击、拖拽与 hover；如需自行选中文本，按住 Shift 拖拽即可（macOS 上 Option 同样能强制选中）。
- 紧凑的性能总览回归：点击 CPU/内存指示器可打开实时图表、最耗资源的六个进程，以及工作负载合计，并可跳转到完整监控面板。
- 工具栏的编辑器按钮现在一次点击即可打开默认编辑器，并可通过单独的菜单切换编辑器。
- 修复了刚启动后新开的终端标签页偶尔会丢失该会话最早输出的问题。
- 修复了 Cordy 收进系统托盘时弹出的退出确认窗口无法填满自身窗口的问题。
- 从 CSV 导入密码时，文件选择器现在会打开在你配置的下载目录。
- Windows 安装包仍未签名，首次运行仍会触发 SmartScreen 警告；性能监控面板的 Workload 视图仍仅限 Windows。

### 0.3.0 — 2026-09-04

- Cordy 发布了第一个安装包：本版本附带 Windows 安装包（macOS 与 Linux 安装包由同一套流程构建，但实际使用经验还很少）。
- 导入仓库与新建工作区现在都只需一次按键。
- 终端预设现在支持分屏启动：右键预设即可把对应 agent 开在当前标签页旁边，而不是替换它。
- 全新的性能监控面板可以准确说明 Cordy 的内存到底被应用本身还是 coding-agent 工作负载占用，细到每个进程。
- 设置中现在可以从本机实际安装的 shell 中选择默认 shell（PowerShell 7、命令提示符、Git Bash、各个 WSL 发行版，或自定义命令）。
- 侧边栏新增全项目搜索，支持大小写匹配、整词匹配与正则表达式。
- 文件图标现在出现在所有列出文件的地方，并可选择包括彩色品牌图标的多种图标主题。
- 右侧的 Files 与 Git 面板不再在 coding agent 写入大量文件时冻住整个应用，Git 面板现在也能可靠列出每一个未追踪文件。
- Windows 安装包未做代码签名，首次运行会触发 SmartScreen 警告；性能监控面板的 Workload 视图仅限 Windows。

### 0.2.0 — 2026-08-12

- 每个工作区现在都自带一块浏览器面板，可在终端旁边打开，带标签页、导航控件与书签栏。
- coding agent 现在也能使用这个浏览器——可以是内置面板，也可以是通过 Cordy for Chrome 扩展连接的真实 Chrome 会话，连接前需要你在弹窗中明确批准配对。
- 启动 coding agent 现在会打开一个受监督的终端标签页，带可见的状态徽章（工作中 / 等待中 / 已退出 / 失败），而不是在后台无声运行。
- 提交历史视图现在可以展开每个提交查看其改动文件，diff 带语法高亮，历史记录支持无限滚动加载。
- 新增的诊断工具可以导出按时间范围过滤的日志包，并查看磁盘占用明细，支持一键清理缓存。
- Cordy 现在会自动检测本机已安装的代码编辑器，不再需要先手动配置才能使用。
- 修复了在内置浏览器中输入的密码可能被 agent 的浏览器工具读取回去的问题；凭据类字段现在已从该读取路径中排除。
- 修复了在内置浏览器中打开 URL 有时会静默失败的问题（发生在浏览器已经归一化过地址的情况下）。

### 0.1.3 — 2026-07-15

- 修复了终端中三个独立的中文输入法吞字问题，其中一个会在组词前静默截断已输入的文字。
- 关闭了模拟键盘的语音输入工具，现在又能正常粘贴进终端了。
- 完全退出并重新打开 Cordy，不再会损坏被恢复终端的显示。
- Source Control 视图现在能可靠地在暂存、丢弃或拉取改动之后与工作树保持同步。
- 新增 Zen 模式、Commit Graph 标签页、文件树中的 git 状态标注，以及可重命名的终端标签页。
- 新增本地崩溃与日志诊断，一切都留在你自己的设备上，不会上传。
- 与 0.1.2、0.1.1 一样，本版本为纯源码发布：不附带打包安装程序。

### 0.1.2 — 2026-07-12

- 从系统托盘恢复或刷新页面后，Cordy 现在会回到你实际离开时所在的工作区，而不是总是最近创建的那个。
- 终端中的中文输入法输入再次变得可靠，长句不再吞字，候选窗也不再乱跳。
- 切回某个终端时，不再会短暂显示丢失光标的陈旧画面。
- Windows 终端现在运行在现代的、经微软签名的 Windows Terminal 引擎上，Codex 等工具现在能正确识别浅色/深色主题。
- 与 0.1.1 一样，本版本为纯源码发布：不附带打包安装程序。

### 0.1.1 — 2026-07-11

- 修复了重新打开窗口或从托盘恢复后，shell 启动输出出现重复的问题。
- 全屏终端界面（编辑器、监控面板、coding agent 等）现在能在刷新或从托盘恢复后被正确重建，而不是只等下一次刷新才重绘。
- 本版本为纯源码发布：打包流程当时处于暂停状态，不附带安装程序。

### 0.1.0 — 2026-07-11

- Cordy 首个公开版本——一个面向 Windows、macOS 与 Linux 的本地优先个人 AI 编码工作台。
- 可以并排启动并监督多个 CLI coding agent（Claude Code、Codex、Cursor Agent、Gemini CLI、OpenCode 等），每个都在自己独立的、基于 Git worktree 的工作区中运行。
- 后台 runtime 让终端、TUI 与运行中的 agent 在应用刷新、窗口崩溃，以及（Windows 上）关闭到系统托盘期间持续存活。
- 内置命令面板、Files/编辑器面板、支持按 hunk 暂存的 Git 面板、按工作区独立的笔记本，以及可自定义的主题。
- 实时状态监控、按 agent 聚合的 diff，以及通知功能，让你随时知道哪个 agent 完成了任务或需要你的关注。
- 一切都保存在你自己的电脑上；Cordy 不存储、不代理、也不读取你 agent 的提示词或回复，也不内置自己的聊天机器人。
- 本版本为纯源码发布：不附带打包安装程序。
- 尚无代码签名证书，Windows SmartScreen 与 macOS Gatekeeper 会在首次启动时发出警告；暂无自动更新；全屏终端界面在刷新后会等待下一次更新才重绘，而不是被完整还原。

## Cordy 浏览器扩展

### 2.5.4 — 2026-09-16

- 更新了随扩展一起提供的隐私政策（中英文版本），更清楚地区分"本地浏览数据"与你通过 @ 提及主动附加、或由 AI 工具检索到的内容，包括历史记录上下文中携带的标签页 / 书签标题、网址与访问时间。
- 移除了关于 AI 书签标签与自动分类的过时描述，并删除了"历史记录与标签页数据永不离开设备"这一不准确的说法。
- 说明了 API 密钥如何用于向所选 AI 服务商验证你的请求身份、使用云端 AI 时适用的数据使用同意机制，以及"设备本地推理"与"你自行配置的运行时端点上运行的 AI"之间的区别。
- 本版本即当前在 Chrome 应用商店上架的版本。请从商店安装——本仓库没有发布扩展的构建包。

### 2.5.3 — 2026-09-16

- 移除了自定义新标签页（其中的时钟、壁纸和快捷链接），让扩展专注于核心生产力功能。
- 将搜索功能移回 Dashboard，并恢复了独立的导航入口；仍可将搜索范围限定为书签、历史记录、已打开的标签页、稍后读或收藏夹，普通网页搜索继续使用你在 Chrome 中设置的默认搜索引擎。
- 搜索结果现在会在新标签页中打开，或聚焦到已存在的匹配标签页，同时保持 Dashboard 搜索页面处于打开状态，方便继续搜索。
- 移除新标签页功能不会删除你此前保存的壁纸或偏好设置数据。
- 更新了 Chrome 网上应用店的商店页面与截图，移除了与新标签页相关的宣传内容。

### 2.4.4 — 2026-09-10

- 重新设计了侧边栏导航：导航项顺序固定、可预期，并清晰高亮当前所在页面，图标与间距也更紧凑。
- 当前选中的导航项现在会通过一条连续的高亮线与对应页面的工具栏连接起来。
- 修复了一个问题：关闭提示气泡时页面布局会短暂跳动。
- "当前 / 历史 / 稍后读 / 收藏夹"标签移动到了页面工具栏右侧，数量以数字形式显示在对应图标旁。
- 为侧边栏和 Dashboard 都新增了完整的外观设置：可选择浅色、深色或跟随系统的主题，并从八款内置主题（包括 Catppuccin、Nord 和 Dracula）与四种字体中挑选，设置会在扩展内同步，且可离线使用。
- 所选字体会应用于界面文字与正文内容，代码部分仍保持等宽字体；新标签页的壁纸设置仍可独立配置，不受影响。
- 为新的外观设置增加了上下文帮助说明，并提供"仅重置外观"的选项，支持全部 12 种语言及窄窗口布局。

### 2.4.3 — 2026-09-08

- 数据清理工具不再删除你的书签：无论是完整清理还是选择性清理，现在都不会触及书签数据和 Chrome 中的书签；删除 API 密钥仍是一个单独、明确的选项。
- 修复了危险操作按钮颜色显示不正确的问题，现在即使没有任何聊天记录，也可以清理应用数据。
- 重新整理了侧边栏的书签工具，归纳为更清晰的"刷新""视图"和"更多"按钮，排序与折叠等操作移入菜单中。
- 命令面板现在会跟随你的浅色 / 深色 / 系统主题变化，并在其搜索栏中显示 Cordy 的标志。
- 为总览、搜索、AI 管理、浏览历史、稍后读、书签和设置等页面添加了页面标题，方便识别当前位置。
- 重新设计了 AI 服务商设置界面：改为可搜索的服务商列表，一次只展开一个编辑区域，并新增了单独的"功能模型"标签页，用于选择各功能对应的模型，整体新增了上下文帮助说明。
- 现在可以为同一条消息生成并比较多个回答版本，也可以基于某个回答继续对话或编辑之前的提问来创建独立分支，而不会丢失原有对话线索。
- 翻译设置（运行方式、源语言 / 目标语言、上次使用的模型）现在会在会话间保留；长文本翻译不再被中途截断，失败时会明确提示是哪部分内容出了问题。

### 2.4.2 — 2026-09-04

- 重要提示：本版本需要 Chrome 148 及以上版本（此前为 142），因为它依赖 Chrome 内置 AI 功能，而该功能要到 148 版本才完整可用；Chrome 142–147 将无法安装或更新本扩展。
- 将 Dashboard、侧边栏设置、翻译功能与聊天输入框中使用的模型选择器统一为同一套组件，服务商状态提示更清晰，价格现在以"每百万 token 的美元价格"正确显示。
- 翻译：当源语言与目标语言相同时，文本会原样直接返回，不再被拒绝翻译或不必要地计入云端 / 本地模型的用量；翻译工作台还新增了语言互换按钮和可拖动调整比例的左右分栏视图。
- 书签：工具栏上新增一个小徽标，用于提示当前页面是否已被保存；书签侧边栏新增了真正的树形视图，面对较大规模的书签库依然流畅；收藏夹起始页的加载速度明显更快。
- 数据管理：自动清理默认改为"仅提示，不删除"，并可以完全关闭；"删除所有数据"现在会真正删除其所声称的全部内容，API 密钥则作为单独的可选勾选项；删除某个书签时，始终会同步从 Chrome 中删除。
- 聊天：你发送的消息现在会按原样渲染；"清空"命令重置视图时不再出现跳动；修复了几种消息链或附件可能被静默丢失或卡住的情况。
- 安全性：撤销某个网站的访问权限后，已经在该网站已打开标签页上运行的 Cordy 脚本会立即失去访问云端 AI、页面内容和语音朗读功能的能力；死链检测不再向你尚未授权的网站发送请求。
- 安装包体积从约 50MB 缩减到 32MB，并修复了多个本地音频与 AI 播放相关的问题，其中包括一个此前被静默判定为无效而遭拒的语音包文件。

### 2.4.1 — 2026-08-13

- 修复了一个严重问题：离线状态下的"清理失效链接"功能可能会将书签库中的所有书签都预先勾选为待删除，包括那些当时只是暂时无法访问、实际上仍然有效的链接；现在只有被确认真正失效的书签才会被预先勾选，其余书签会保持可见，仍可单独删除。
- 修复了备份恢复功能完全无法使用的问题，只要备份中包含聊天记录，此前恢复必定失败，现在恢复功能可以正常工作。
- 修复了"重复书签"清理步骤此前完全无法使用的问题，同时避免了修复后可能把每一份重复书签全部删除、一份都不保留的隐患。
- 安全性：此前，只要某个网站获得过访问授权，该网站上运行的脚本就可以读取你的书签、浏览历史和已打开的标签页，即使你从未打开过 Cordy 的命令面板；如果脚本已经在运行，即便你事后撤销了该网站的授权，它仍能继续读取这些数据。这两个漏洞现已修复：读取操作现在要求你刚刚主动打开过命令面板，且撤销某网站权限会立即终止其访问能力。
- 安全性：修复了恶意页面或书签标题可能利用某种手段绕过 Cordy"不可信内容"边界的问题，并限制了 Cordy 工具可以打开或聚焦的浏览器地址与窗口范围。
- 修复了在对话过程中切换 AI 模型会导致可用上下文窗口被静默缩小的问题，这可能造成你发送的部分内容在不知情的情况下被丢弃。
- 编辑已保存标签页的功能现在真正可用了（此前从功能上线起一直只会显示"即将推出"的提示）;"收藏夹"与"稍后读"中的批量操作现在也可以正常从界面进入使用。
- 为全部 12 种支持语言翻译了约 5500 条此前只有英文的界面文本，并为阿拉伯语新增了真正的从右到左排版支持。

### 2.4.0 — 2026-08-12

- 移除了 AI 书签自动打标签、自动分类以及"洞察"摘要功能；你现有的书签和标签不受影响，但从本版本起无法再生成新的 AI 标签或摘要。
- 完全移除了实验性的跨设备"浏览器连接器"配套功能；该功能从未在正式版本中上线过，因此大多数用户不会感觉到任何变化。
- 由此，Cordy 不再申请"与本机应用程序通信""显示通知"和"调试此浏览器"这三项权限，安装时的权限提示进一步缩减。
- 书签：书签列表现在支持勾选多选，可批量重命名、移动、打标签和删除，并支持按列排序及显示完整的文件夹路径列；"打开全部"新增了在当前窗口 / 新窗口 / 标签组中打开三种方式，数量较多时会先弹出确认提示；书签图标现在可从 Chrome 本地直接读取，加载更快。
- 收藏夹（保存的标签页分组）新增了置顶、归档、去重、粘贴导入、四种复制格式，以及浏览器启动时自动重新打开的选项。
- 浏览历史现在按天分组显示（今天 / 昨天 / 更早日期），并提供最近 3 天、7 天、30 天或自定义时间范围的快捷筛选；对话历史也新增了同样的日期筛选功能。
- 为聊天新增了单轮模式开关，需要进行一次干净的单独问答、不想携带此前上下文时可以开启。
- 刷新了整体视觉设计：统一了图标按钮样式，简化为两组结构的侧边栏，并重新设计了日期范围选择器。

### 2.3.3 — 2026-07-16

- 重新梳理了设置页面的信息结构，并新增了可导出的本地日志，方便你查看某次会话期间发生了什么。
- 聊天界面新增上下文用量环形指示器，可直观看到当前对话占用了模型上下文窗口的多少；对话现在会根据你的第一条消息自动命名。
- 重新打开或切回聊天面板时不再丢失上下文；因回复过程中被中断（例如浏览器重启）而未完成的消息，现在会被明确标记为"未完成"，而不是看起来像已经结束。
- 语音朗读的播放速度现在被限制在合理范围内，调整后会立即生效。
- 书签新增了 AI 自动生成的标签与摘要，以及按域名分组和网格两种新的视图方式。
- 站点访问权限改为按需申请，不再在安装时自动获得；同时修正了随扩展提供的隐私政策中的若干不准确表述。
- 为上下文用量环形指示器、书签、设置等近期新增功能补全了全部 12 种支持语言的翻译。

### 2.3.2 — 2026-07-12

- 对聊天引擎进行了整体重写：无论是编辑历史消息、还是基于某个回答继续对话所产生的分支，现在都能在各处被正确处理；同一对话在两个窗口中同时打开时，也不会再导致消息顺序错乱。
- 中途停止一次回复不会再丢失内容——已生成的部分会被保留下来；被你拒绝执行的工具调用现在会明确显示为"已拒绝"，而不是看起来卡住或悄悄成功。
- 打开包含数百条历史消息的聊天明显更快，更早的消息会按需加载。
- 首次向云端 AI 服务商发送内容前，会弹出一次性的同意提示，说明将会分享哪些内容；你可以随时在"设置 → 数据与隐私"中撤回该同意。
- 站点权限改为可选：升级后，划词助手等页面内功能需要在"设置 → 通用"中重新点击启用一次（侧边栏功能不受影响）；划词弹出窗口也可以整体关闭。
- 本地 AI 与语音朗读修复：修复了某些表情符号或外语文本导致的崩溃问题，下载本地 AI 模型现在可以正常取消，并移除了印地语本地语音包——印地语文本现在改用系统自带语音朗读。
- 扩展的安装包体积从约 97MB 缩减到 25.5MB。
- 浏览历史、稍后读、收藏和对话历史现在都并入了主 Dashboard；书签也获得了真正的文件夹树结构，可以通过拖拽重新整理。

### 2.3.1 — 2026-06-24

- 页面内容以及你在页面上选中的文字，在发送给 AI 之前现在始终被当作不可信输入处理，从而堵住了恶意页面内容借此劫持 AI 指令的一种途径。
- 新增了一个总开关，可以完全禁用 AI 使用浏览器工具（例如打开标签页）的能力；同时默认为任何写入数据或涉及敏感操作的工具调用开启人工审批。
- 新增一键"收纳所有标签页"功能——一次性保存并关闭当前所有已打开的标签页，恢复某个标签页时会自动将其从已保存列表中移除；可通过扩展图标右键菜单或快捷键使用。
- 新增了对话历史的全文搜索功能，以及可以直接在已打开的标签页上运行的快捷 AI 操作。
- 新增了按单个对话统计的 token 用量视图，翻译结果现在也可以多次结果并排对比查看。
- 历史记录与书签的访问权限改为按需申请，不再是安装时必须授予的权限；备份文件现在需要先通过格式与版本校验，才能被导入。
- 性能：页面内的划词功能在闲置一分钟后会自动卸载以节省内存；流式回复期间会保持扩展处于活跃状态，长对话不再出现请求失败的报错；回复文字的流式输出也更加顺滑，不再是一段一段地卡顿式出现。
- 修复了若干细节问题，包括使用触控板切换对话标签页，以及划词菜单在快速关闭并重新打开时可能卡住的问题。

### 2.3.0 — 2026-06-15

- 现在可以在聊天中输入 @ 并选择一个已打开的标签页、书签或历史记录条目，将浏览器内容直接带入对话；被引用页面的内容会自动作为上下文提供给 AI。
- Dashboard 中新增了独立的翻译页面：可选择源语言与目标语言，以及默认、本地或云端翻译引擎，支持长文本分段翻译并实时流式输出结果，随时可以停止、重试、复制或清空。
- 聊天上下文现在根据实际的 token 预算来组装，而不再是固定的历史消息条数，因此能在不超出模型限制的前提下容纳更多相关历史；当有较早的消息因此未被发送时，界面会明确提示，而不是悄悄丢弃；长对话还可以自动压缩为摘要以节省空间，并支持撤销。
- 新增了连接你自己设备或局域网内运行的 AI 模型的能力（支持 Ollama、LM Studio、llama.cpp、vLLM、LocalAI 或任意兼容 OpenAI 接口的服务），作为与 Chrome 内置 AI、浏览器内模型并列的独立分类展示。
- 新增两个云端 AI 服务商（Vercel AI Gateway 和火山引擎的 Ark 平台），以及基于火山引擎语音合成服务的云端语音朗读功能，配备独立的音色库与试听。
- 重新构建了命令面板，改进了搜索排序、新增了最近历史建议和标签页导航，并在执行破坏性操作前增加了内嵌二次确认步骤。
- 标签页面板中的浏览历史现在会去重，并按访问频率与最近访问时间排序，不再是上限 200 条的原始扁平列表；书签现在也会自动同步到 Dashboard 中。
- 性能：AI 助手的页面内界面现在会拆分为独立的较小代码包加载，使普通页面的加载速度更快；打开包含数百条消息的对话速度更快；进行中的回复现在也能在扩展后台进程重启后继续完成，而不会卡住。

## CordyAI

### 0.5.0 — 2026-07-16

- 端到端图片输入，支持图库选择、相机拍摄和应用分享接入，带视觉模型能力检测
- 语音输入支持可编辑转写文本
- 响应流式传输期间的实时 Markdown 渲染
- 动态定价模型目录与 Token 用量分析
- 支持 OpenRouter 推理模型与 Anthropic 提示缓存
- 切换会话、服务商或模型时生成内容保持
- 安全增强：附件不进云备份、iOS 文件静态加密、敏感授权头脱敏、加密凭证存储
- 本次为纯更新日志发布；暂无公开可下载的签名版本

## Tabmori

_尚无公开发布。_
