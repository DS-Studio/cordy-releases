# This repository holds text and scripts only. Build artifacts live in
# GitHub Release assets and must never be committed.

# Release artifacts, staged locally by scripts/publish.mjs
/tmp/
*.exe
*.dmg
*.deb
*.zip
*.AppImage
*.blockmap
*.apk
*.aab
SHASUMS256.txt

# The leak-scan term list. Never commit this: the whole point is that the terms
# the scanner suppresses must not themselves ship in a public repository.
.leakterms

# Tooling noise
node_modules/
.DS_Store
Thumbs.db
*.log
